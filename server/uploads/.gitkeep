# This file exists only so that git tracks the uploads directory while ignoring
# everything inside it. Uploaded resumes are real personal data and must never be
# committed — see the .gitignore rule at the project root.
